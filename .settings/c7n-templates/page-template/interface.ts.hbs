import { StoreProps } from './stores/useStore'

export type {{toUpperFirstName}}IndexProps = {
}

export type {{toUpperFirstName}}StoreContext = {
  prefixCls:string
  intlPrefix:string
  mainStore:StoreProps
  formatMessage(arg0: object, arg1?: object): string,
  projectId:string
}