import React, {
  useEffect, FC
} from 'react';
import { observer } from 'mobx-react-lite';
import {} from 'choerodon-ui/pro';
import {} from '@choerodon/components';

import './index.less';

export type {{toUpperFirstName}}Props = {

}

const prefixCls = '{{prefixCls}}';
const intlPrefix = '{{intlPrefix}}'

const {{toUpperFirstName}}:FC<{{toUpperFirstName}}Props> = (props) => {
  const {

  } = props;

  useEffect(()=>{

  }, []);

  return (
    <div class={prefixCls}>
      {{toUpperFirstName}}
    </div>
  )
}

export default {{toUpperFirstName}}