import React from 'react';
import { StoreProvider } from './stores';
import Content from './Content';
import './index.less';

type {{toUpperFirstName}}Index = {
}

const {{toUpperFirstName}}Index = (props: {{toUpperFirstName}}Index) => (
  <StoreProvider {...props}>
    <Content />
  </StoreProvider>
);

export default {{toUpperFirstName}}Index;

