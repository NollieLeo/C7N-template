import React, {
  useEffect, FC
} from 'react';
import { observer } from 'mobx-react-lite';
import { use{{toUpperFirstName}}Store } from './stores';
import {} from 'choerodon-ui/pro';
import {} from '@choerodon/components';

const {{toUpperFirstName}} = observer(() => {
  const {
    mainStore,
    prefixCls,
  } = use{{toUpperFirstName}}Store();

  useEffect(()=>{

  }, []);

  return (
    <div class={prefixCls}>
      {{toUpperFirstName}}
    </div>
  )
})

export default {{toUpperFirstName}}